List backlog items for the idea identified by `slug`. Returns the same shape as `list_backlog` (`{id, title, body?, status, created, updated, source?, assignee_session?, external_url?, depends_on?, affects?}`, oldest-first).

Use to inspect a sibling idea's open work — e.g. "what's outstanding on the migration idea before I file a new task there?"
