Send text BACK to the orchestrating (root orchestrator) agent. Use this tool when you received a `[Input from Orchestrating Agent]` signal in your transcript and have a response — the orchestrator delegated the turn to you and is waiting for your reply. The text is auto-prefixed with `[Reply from <idea name>]` so the orchestrator can route it back to the right context. Errors if no orchestrator session is currently running.
